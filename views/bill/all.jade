extends ../layout

block scripts
  script(src='/js/create_bill.js')

block content
  h4 Чеки
  if error
    h4(style="color: red")= error
  .container-fluid
    table.table.table-hover
      thead
        tr
          th #
          th ID
          th Магазин
          th Дата
          th Оператор
      tbody
        each item, key in bills
          tr
            th(scope="row")= key+1
            td= item.id
            td= item.doc.shop_title
            td= item.doc.date
            td= item.doc.creator