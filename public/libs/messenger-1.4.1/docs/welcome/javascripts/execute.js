// Generated by CoffeeScript 1.9.1
(function() {
  $.fn.executr = function(opts) {
    var defaults;
    defaults = {
      codeSelector: 'code[executable]'
    };
    opts = $.extend({}, defaults, opts);
    return this.on('click', opts.codeSelector, function(e) {
      var $code, $target, ctx;
      $target = $(e.target);
      $code = $target.parents(opts.codeSelector);
      ctx = window;
      if (opts.setUp != null) {
        CoffeeScript.run(opts.setUp, ctx);
      }
      CoffeeScript.run($code.text(), ctx);
      if (opts.tearDown != null) {
        return CoffeeScript.run(opts.tearDown, ctx);
      }
    });
  };

}).call(this);
